/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run nf-core/hlatyping -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    withName: OPTITYPE {
        // OptiType's ILP peaks at 68 GB on the 93.6M-pair RNA sample. Its process_medium label allows
        // 36 GB, and the single retry only reaches 72 GB, so give it headroom here rather than lose the
        // full test to an OOM. Typical (HLA-fished, DNA) inputs stay well under 2 GB.
        memory = { 96.GB * task.attempt }
    }
}

params {
    config_profile_name        = 'Full test profile'
    config_profile_description = 'Full-size test exercising OptiType, SpecHLA, HLA*LA (via GRCh38 alignment) and immunotype'

    // Input data for full size test
    input = params.pipelines_testdata_base_path + 'hlatyping/samplesheets/samplesheet_full.csv'

    // All public tools (HLA-HD is licensed -> covered by the dedicated hlahd nf-test job, not here)
    tools  = 'optitype,spechla,hlala,immunotype'
    // UCSC hg38: chr-named (chr6, chrUn_*, *_random) so HLA*LA's knownReferences match; iGenomes ships a pre-built bwa index
    genome = 'hg38'

    // pipeline parameters
    solver = 'glpk'

}
